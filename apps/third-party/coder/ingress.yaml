apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: coder
  namespace: coder
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`coder.nmajor.net`) || Host(`*.coder.nmajor.net`)
      kind: Rule
      services:
        - name: coder
          port: 80