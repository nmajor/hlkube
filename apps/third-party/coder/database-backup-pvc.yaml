apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coder-database-backups
  namespace: coder
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
