apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookstack-mariadb
  namespace: bookstack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-single-replica
  resources:
    requests:
      storage: 10Gi
