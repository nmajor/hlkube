apiVersion: v1
kind: Service
metadata:
  name: postiz
  namespace: postiz
  labels:
    app: postiz
    component: main
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: postiz
    component: main
