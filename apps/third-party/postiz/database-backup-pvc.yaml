apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postiz-database-backups
  namespace: postiz
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
